<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiSaveUpDesktop.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiSaveUpDesktop"
    xmlns:view="clr-namespace:MauiSaveUpDesktop.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab>
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
        </Tab>
        <Tab>
            <ShellContent
                Title="Settings"
                ContentTemplate="{DataTemplate view:Resultate}"
                 />
        </Tab>
    </TabBar>
</Shell>
